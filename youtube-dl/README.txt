source: https://github.com/rg3/youtube-dl.git

Pre-requisites:
    1. Python, cx_freeze need to be pre-installed.

Usage:
Disable cloning youtube-dl and freezing it by passing:
mvn clean install -Ddisable_freeze=true